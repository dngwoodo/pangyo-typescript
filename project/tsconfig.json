{
    "compilerOptions": {
        "allowJs": true, // 점진적으로 적용하기 위한 설정. js파일도 컴파일해줌
        "target": "ES5",
        "outDir": "./dist",
        "moduleResolution": "Node",
        "lib": ["ES2015", "DOM", "DOM.Iterable"],
        "noImplicitAny": true,
        "typeRoots": ["./node_modules/@types", "./types"],
        "strict": true // strict에 관련된 모든 옵션이 true가 된다.
    },
    "include": [
        "./src/**/*" 
    ] // src밑에 있는 모든 파일들
}